export default async function(context) {
    if (context.req && context.req.headers) {
        // if (/(iPhone|iPad|iPod|iOS|Android)/i.test(context.req.headers["user-agent"])) {
        //     return context.redirect("https://www.maijimeng.com");
        // }
    }
}
